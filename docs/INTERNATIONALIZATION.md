# 国际化约定

> 状态说明（2026-07）：当前 `frontend/` 是干净室重写后的新前端，**尚未实现 i18n**——
> 界面文案为硬编码中文，`frontend/src` 下不存在 `i18n/` 目录、locale 文件或 `t()` 调用。
>
> 本文此前描述的 locale key 体系（`zh-CN.json` / `en-US.json` / namespace 约定）属于
> 已删除的旧前端，相关内容已全部移除，仅保留本状态记录。
>
> 新增功能的文案直接写中文即可。若未来重启国际化，应基于当前前端重新设计 key 结构与
> 接入方式，不要引用旧前端的实现。
