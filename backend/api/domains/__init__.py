"""Domain router aggregation for the FastAPI service.

These modules provide a higher-level grouping for `backend/api/router.py` so the
API surface can be navigated by domain (system/auth/workspace/generation/tasks/integrations)
without forcing a flat router include list.
"""

