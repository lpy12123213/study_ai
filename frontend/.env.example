# 前端构建/开发环境变量
# 开发时复制为 frontend/.env 使用；构建时也可作为环境变量注入。不要提交真实值。

# 后端 API 基础地址。
# 默认留空：同源相对 /api（配合后端托管 frontend/dist 或 Vite dev proxy 使用）。
# 跨站部署：设为后端 origin，例如 https://api.example.com；
#   同时需要后端配置 CORS_ORIGINS allowlist 与 AUTH_COOKIE_SAMESITE=none（隐含 Secure，要求 HTTPS）。
# 修改后需重新构建前端。
VITE_API_BASE_URL=

# Vite dev server 的 /api 代理目标（仅开发模式生效）。
# 默认 http://localhost:8000。
VITE_DEV_PROXY_TARGET=http://localhost:8000
