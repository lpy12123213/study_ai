[tool.ruff]
target-version = "py310"
line-length = 120
extend-exclude = [
  "venv",
  ".venv",
  ".local",
  "frontend/node_modules",
  "frontend/dist",
  "archive",
  "playground",
  "artifacts",
  "data",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
